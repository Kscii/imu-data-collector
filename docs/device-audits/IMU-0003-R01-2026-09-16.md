# IMU-0003-R01 production profile audit — 2026-09-16

This record separates observed identity, family assumptions, and the production exception for
`acce&gyro_C18A101C`.

## Identity and observed runtime facts

| Field | Value | Evidence status |
|---|---|---|
| Logical sensor SN | `IMU-0003-R01` | Project-assigned |
| Hardware asset | `IMU-0003` | Project-assigned |
| Advertised name | `acce&gyro_C18A101C` | Previously observed runtime configuration |
| Public BLE address | `E8:3D:C1:8A:10:1E` | Previously observed runtime configuration |
| Address type | public | Previously observed runtime configuration |
| Protocol | `acce_gyro_abf0_v1` / notify `0xABF2` | A prior five-second probe recorded 251 samples |
| Expected rate | `50 Hz` | Prior probe status: `observed_abf2_251_samples_in_5s_2026-09-08` |
| Firmware | stock, exact version unknown | Not independently read from the device |
| GATT fingerprint | unknown | Not copied from another physical device |

The operator reports that this device has the same visible board, sensor model, and enclosure as
`IMU-0002-R01`. Appearance does not independently prove the MEMS identity, configured ranges,
firmware, PCB orientation, per-device offset, temperature drift, or GATT fingerprint. Those are
accepted as a named family-level production risk rather than silently treated as measured facts.

## Production SI profile

The candidate uses the official Bosch BMI160 manufacturer-typical sensitivities:

- `16384 counts/g` at the assumed `±2 g` range;
- `16.4 counts/(°/s)` at the assumed `±2000 °/s` range;
- accelerometer and gyroscope bias arrays `[0, 0, 0]`, meaning no external offset compensation;
- raw axis order `[0, 1, 2]` and runtime axis signs `[1, 1, 1]`;
- the same physical-face coordinate descriptions as `IMU-0002-R01`.

Official source: [BMI160 Datasheet](https://www.bosch-sensortec.com/media/boschsensortec/downloads/datasheets/bst-bmi160-ds000.pdf).

The representative report for `IMU-0002-R01` is preserved and direction-corrected under
[`evidence/`](evidence/). The corrected derivative has SHA-256
`6bd33daeff85d3fb7a44c6c950014c4b322e2a7430e4504d75cede264c043420` and records a corrected
gyroscope fit median of `16.401 counts/(°/s)`. Its original HDF5 sources are unavailable, its
gyroscope validation integrations cannot be recomputed, and the test fixture was taped to a power
bank. It is supporting family evidence, not a calibration of this physical device.

Marking this device's SI profile as verified and allowing `prod` requires the administrator to
accept all of the following explicitly:

1. datasheet nominal sensitivities replace per-device scale fitting;
2. no external per-device bias compensation is applied;
3. protocol, ranges, and axis integration are assumed to match `IMU-0002-R01`;
4. the representative report lacks its raw HDF5 sources and independent gyro validation;
5. any later range, firmware, PCB orientation, or protocol difference requires a new device
   revision or a replacement approved Snapshot.
