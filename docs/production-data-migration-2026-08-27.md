# 2026-08-27 首次生产数据迁移清单

本文固定首次上线前的只读盘点和执行边界。它不是执行日志；在实际命令结果、操作者和时间补齐
前，不能据此声称迁移已经完成。

## 1. 只读盘点

2026-08-27 对 `gs://soft3888-label` 的只读枚举结果：100 个对象，共 407,039,540 bytes
（约 388.18 MiB）。组成如下：

| 类型 | 对象数 | 处理方式 |
| --- | ---: | --- |
| 15 条校准录制的 capture 制品 | 60 | 迁入只读校准证据区，校验后删除普通录制区副本 |
| 正式验收录制 `20260827T014121.043236Z_xfan0282` | 4 | 保留为普通标注任务 |
| review | 16 | 校准 review 随源录制移除；验收 review 保留 |
| index receipt | 18 | 15 条校准回执随迁移移除；验收回执保留；2 条孤儿回执进入保留期清理 |
| 能力合同 | 1 | 保留 |
| 部署冒烟对象 | 1 | 保留 |

没有发现校准证据、验收录制之外的云端 capture。不得使用模糊前缀批量删除。

## 2. 必须归档的 15 条校准证据

以下 ID 必须与 `configs/calibration-evidence.yaml` 完全一致：

1. `20260826T143249.420984Z_xfan0282`
2. `20260826T143451.763656Z_xfan0282`
3. `20260826T143553.112844Z_xfan0282`
4. `20260826T143715.290019Z_xfan0282`
5. `20260826T144030.144852Z_xfan0282`
6. `20260826T144127.783098Z_xfan0282`
7. `20260826T144352.550677Z_xfan0282`
8. `20260826T144454.002771Z_xfan0282`
9. `20260826T144614.183656Z_xfan0282`
10. `20260826T154758.057276Z_xfan0282`
11. `20260826T154943.739216Z_xfan0282`
12. `20260826T155132.366440Z_xfan0282`
13. `20260826T161802.637311Z_xfan0282`
14. `20260826T161857.507603Z_xfan0282`
15. `20260826T162007.901513Z_xfan0282`

每条归档必须包含原始 H5、MKV、预览 MP4、冻结源 manifest 和归档 manifest。服务逐项校验
SHA-256，四类源制品全部可重新读取后才允许删除普通录制区副本。

## 3. 精确清理候选

云端只有两条已失去源 manifest 的索引回执：

- `index-receipts/20260827T013226.841887Z_xfan0282.json`
- `index-receipts/20260827T013600.233422Z_xfan0282.json`

它们不是录制数据，不影响训练或标注。默认垃圾回收保留 7 天后才删除，避免把上传与扫描短暂
错位误判为垃圾。若首发当天必须立即移除，应另做一次精确 generation 校验和人工删除，不要把
全局保留期临时改成零。

本机还有未上传的 `013226`、`013600`、`013825`、`013842` 录制和 `_diagnostics`。本清单不
授权删除本机文件；待确认不再用于复现后，使用采集页面逐条二次确认删除。

## 4. 执行顺序与验收

部署包含归档能力的新版本后，在服务器先预演：

```bash
sudo -u kscii /opt/imu-annotation/current/.venv/bin/imu-annotation \
  --config /etc/imu-annotation/config.yaml archive-calibration-evidence
```

预演必须只列出上述 15 条，且没有缺失制品或哈希错误。随后才执行：

```bash
sudo -u kscii /opt/imu-annotation/current/.venv/bin/imu-annotation \
  --config /etc/imu-annotation/config.yaml \
  archive-calibration-evidence --apply --delete-source
```

理论对象数变化：`100 + 75 个归档对象 - 90 个校准源对象 = 85`；两条孤儿回执超过 7 天并
由垃圾回收移除后为 83。对象数只用于发现漏项，最终验收仍以对象键、generation、SHA-256 和
页面行为为准。

迁移后必须验证：

- 校准页恰好显示 15 条证据，每条可播放视频、下载 H5、复制 ID；
- 15 条证据不再出现在普通标注队列；
- `20260827T014121.043236Z_xfan0282` 仍在普通标注队列，review 未变化；
- `contracts/annotation-capabilities.json` 与部署冒烟对象仍存在；
- 垃圾回收先运行 dry-run，只报告符合保留期的孤儿对象。

对象存储保留 7 天 soft delete 作为误操作恢复窗口。执行后在本文追加实际命令输出摘要、对象
数、执行时间和操作者，不修改上面的原始盘点。
