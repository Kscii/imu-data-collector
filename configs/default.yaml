data_root: "~/IMUData"
catalog_path: "~/.local/share/imu-data-collector/catalog.sqlite3"
activity_taxonomy_path: "configs/activities.yaml"
server_host: "127.0.0.1"
server_port: 8765
minimum_free_gib: 20

identity:
  # 参与者和标注者共用同一白名单；修改后需重启本地服务。
  allowed_unikeys:
    - "rkim6933"
    - "zche0826"
    - "jzho8728"
    - "jzha9115"
    - "xfan0282"
    - "yniu0950"
    - "hche5673"
    - "jmia0254"
    - "xliu0452"
  admins:
    - "xfan0282"

auth:
  # 本机开发固定为 local；生产服务器必须改成 iap，且不允许自动回退。
  mode: "local"
  iap_audience: null

storage:
  # 当前只实现本地对象存储；未来 GCS 实现沿用同一个对象键接口。
  backend: "local"
  root: "~/.local/share/imu-data-collector/objects"

imu:
  name: "CW12EU-T"
  address: "83:FC:90:14:1E:A4"
  notify_uuid: "00002ae1-0000-1000-8000-00805f9b34fb"
  expected_rate_hz: 25.0
  expected_rate_status: "short_probe_observed_2026-08-25_pending_long_run"
  frame_size_bytes: 16
  accel_counts_per_g: null
  gyro_counts_per_dps: null

video:
  device: null
  # 首次由 WebUI 选择稳定 camera_id；device 仅用于显式调试覆盖。
  camera_id: null
  width: 1920
  height: 1080
  requested_fps: 30
  input_format: "mjpeg"
  bitrate: "6M"
  maxrate: "8M"
  preview_width: 640
  preview_fps: 10
  vaapi_device: "/dev/dri/renderD128"

upload:
  enabled: false
  remote: "gdrive"
  remote_root: null
