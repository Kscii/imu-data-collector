# 实施与验证 TODO

状态约定：`[x]` 已实现并有自动化或主机证据；`[ ]` 未完成；`[~]` 已实现但尚缺真实设备/人工验收。

## 跨仓库定位与协作约束

跨仓库总体开发顺序以 `imu-fall-benchmark/TODO.md` 为准。本仓库只负责真实 IMU/视频采集、同步、人工标注、版本化导出和数据可信度证据，不负责训练模型、比较算法或实现 Android 推理产品。

- `EPIC` 表示可能改变采集流程、HDF5 合同、隐私边界或交付范围的大任务；范围不明确时，Codex 必须先向用户确认目标、取舍和验收标准。
- `TASK` 表示边界明确、可以独立实现和验收的小任务。
- TODO 不代表对删除数据、推送远程、发布服务、修改云端状态或扩大工作范围的授权。
- 功能代码必须保留必要的自动化测试；真实设备、浏览器和云端验收需要与自动化测试分别记录。
- 参与者原始数据、视频和私有回放样例不得为了测试方便进入公开版本控制。
- Arch 到 Windows/WSL 的个人 SSH 适配器只服务模型开发，不属于采集仓库，也不得进入 Git。

### 总体阶段镜像

- [x] `EPIC C0`：建立 raw-first 采集、同步、标注和不可变导出的基础闭环。
- [~] `EPIC C1`：用真实 prod 录制验收训练用 HDF5 v3 导出合同和来源追踪。
- [ ] `EPIC C2`：完成设备长时稳定性、断线恢复和 30 分钟以上采集验证。
- [ ] `EPIC C3`：按审核后的采集方案持续扩大真实 ADL 与跌倒区间数据。
- [ ] `EPIC C4`：向模型仓库发布不可变、带版本和摘要的数据快照；模型仓库不得直接读取采集中的活动文件。

### 桌面跨平台采集

- [x] 把主机目录、资源定位、文件锁、BLE 后端和摄像头输入从 Linux 实现中拆分；Linux 使用 BlueZ/V4L2，Windows 使用 WinRT/DirectShow，macOS 使用 CoreBluetooth/AVFoundation。
- [x] capture H5 schema 1.6 冻结 OS、架构、Python 单调时钟实现、BLE/视频后端、本地设备标识、摄像头控制策略和视频时间映射；标注端继续兼容 1.5 与 1.6。
- [x] 2026-08-28 Windows 10 x64 局域网真机源码验收：138 项 Python 测试与 Ruff 通过，前端双构建通过；PyInstaller onedir 独立 EXE 能找到随包 FFmpeg，并完成 `doctor`、DirectShow 枚举及全新数据目录下的健康接口验收。
- [~] Windows 双入口已实现：无控制台托盘程序负责双击启动、单实例、打开 WebUI 和安全退出，控制台程序保留诊断命令；Windows 10 已验证托盘进程进入图形 Session 1、无控制台窗口、健康接口 200 且重复启动仍为单实例，仍待人工点击菜单验收“打开页面”和优雅退出。
- [~] Windows 10/11 x64 未签名试用安装包与 GitHub Release 工作流已实现；Windows 10 已完成
  罗技约 30 FPS 预览、CW12EU-T WinRT 通知、短录制 H5/MKV、Google 登录和上传验收。仍待
  Windows 11、安装/卸载、长录制和断线恢复人工验收。
- [~] macOS 13+ Intel/Apple Silicon 分架构菜单栏 `.app`、ad-hoc 签名 DMG、CoreBluetooth
  本机 UUID 绑定、AVFoundation 真实模式、VideoToolbox 实测回退和 GitHub Release 门禁已实现；
  等待 Intel 真机完成权限、BLE、罗技摄像头、完整录制、Keychain 登录上传和故障恢复验收。
  Apple Silicon 当前只有 CI 证据；未公证测试版不得描述成正式对外发行版。
- [x] 桌面 Google OAuth + PKCE、系统凭据库、上传代理与单录制 GCS resumable upload 已实现；源码默认本机归档，发布安装包由 CI 注入 broker 配置。代理在 manifest-last 前核对三个对象的大小和 SHA-256。
- [~] Google Auth Platform Desktop client、GitHub 公开变量与服务器私有 OAuth secret 已配置；
  Windows 登录、上传和标注端接收已验收，仍待网络中断后的断点续传验收。
- [x] Windows 10 已使用真实罗技摄像头验证约 30 FPS 输入与浏览器预览，并完成短录制。
- [~] Windows 10 已在 Arch 完全断开后按固定 public address 取得 GATT 服务、订阅通知并在 WebUI
  显示约 25 Hz 原始曲线；仍待断电、超距和自动恢复验收。

### 当前跨仓库小任务

- [ ] `TASK C1.1`：完成至少一条真实 prod 录制的同步、区间标注、审核和 aligned HDF5 v3 导出。
- [ ] `TASK C1.2`：用 schema 校验和独立来源/摘要校验确认导出物可被模型仓库接收。
- [ ] `TASK C2.1`：完成设备持续至少 30 分钟的包率、间隙、漂移、重连和资源占用统计。
- [ ] `TASK C4.1`：与模型仓库冻结快照 manifest、版本号、SHA-256 和撤回/替换规则。

## P0：采集数据可信度

- [x] 建立独立项目、配置、Python 后端与 React 本地 WebUI 骨架。
- [x] 设计 raw-first HDF5：BLE 原始通知、原始 6 轴计数、包接收时间、推算样本时间、视频逐帧 PTS、同步锚点、标注修订。
- [x] 增加显式 `data_tier=test|prod`；`test` 与缺失级别的旧条目永久禁止训练，不从文件名猜测资格。
- [x] 电脑端通过 BlueZ LE 枚举并确认 `0x2AE1` 可通知；另有 `0xAE02`、`0x2A37` 两个通知候选，但当前六轴候选数据来自 `0x2AE1`。
- [x] 实测 `0x2AE1` 每包 400 byte，可稳定拆为 25 个 16-byte 候选帧；六列已由六面静止和三轴旋转确认为加速度与陀螺仪，4-byte 尾部含义仍未知并无损保留。
- [ ] 实测设备持续至少 30 分钟，统计真实样本率、包批量、丢包/间隙、漂移和重连行为。
- [x] 增加 IMU-only 分阶段表征 H5、相邻 JSON 报告、包/时序/噪声/尾部统计与校准候选；诊断文件固定禁止训练。
- [x] 2026-08-25 真实链路冒烟：10 包、250 样本、25.00384 Hz、0 间隙、0 回调丢弃、0 解析错误；姿态未控制，所以未生成校准候选。
- [x] 2026-08-25 按键面朝上静态 10 分钟：600 个 400-byte 包、15,000 样本、整体拟合 25.00213 Hz、0 长间隙、0 回调丢弃、0 解析错误；第三个加速度候选列中位数 4045 counts。用户说明按键面朝下可摆放得更稳定，后续成对分析需比较两侧噪声。
- [x] 2026-08-25 按键面朝下静态 10 分钟：600 个 400-byte 包、15,000 样本、整体拟合 25.00191 Hz、0 长间隙、0 回调丢弃、0 解析错误；第三个加速度候选列中位数 −4130 counts。
- [x] 按键面 ±Z 成对报告得到第三原始加速度列为设备 Z：主导响应 8175 counts、相对下一列约 40.67 倍；后续六面证据与 4096 counts/g 工程档案一致。
- [~] 2026-08-25 接口面朝上（设备 −X 面）静态 5 分钟：300 个 400-byte 包、7,500 样本、整体拟合 25.00356 Hz、0 长间隙、0 回调丢弃、0 解析错误；第一加速度候选列中位数 −4060，支持其为设备 X 轴候选。第五候选列出现姿态相关宽噪声（1%–99% 约 −69 到 +72，极值 −240 到 +225），等待接口反面实验判断是否复现；本段不用于该列静态噪声基线。
- [x] 2026-08-25 接口反面朝上（设备 +X 面）静态 5 分钟：300 个 400-byte 包、7,500 样本、0 长间隙、0 回调丢弃、0 解析错误；第一加速度候选列中位数 4119。一次 1.360 秒通知到达延迟使主机时间拟合为 24.98171 Hz，但没有缺少应用通知或候选帧。
- [x] 接口面 ±X 成对报告得到第一原始加速度列为设备 X：主导响应 8179 counts、相对下一列约 36.84 倍、`counts/g` 候选 4089.5、零偏候选 +29.5 counts。接口反面第五列的宽噪声结合较小接触面解释为机械微摇，两段不用于陀螺仪静态噪声基线。
- [~] 2026-08-25 手扶吊坠/挂绳端朝上探索段在约 70 秒时按用户要求中断，已从 partial 副本保守收尾：70 包、1,750 样本，第二加速度候选列中位数 −4043；原 partial 保留，回调丢弃数因进程中断标为 unknown。
- [~] 用户更正随后一分钟实际也是“吊坠/挂绳端朝上、全程手扶”，平台已原子更正 H5 阶段并保留旧标签、原 SHA-256、原因与时间。两次手扶吊坠端的加速度中位数 `[93, −4043, −35]` 与 `[104, −4043, −31]` 高度一致，支持第二原始加速度列对应吊坠方向/Y 轴、且该朝上姿态为负号的探索性候选；因手扶抖动，不用于正式 `counts/g`、零偏或噪声基线。
- [~] 2026-08-25 手扶吊坠反面朝上 1 分钟：60 个 400-byte 包、1,500 样本、0 长间隙、0 回调丢弃、0 解析错误；第二加速度候选列中位数 +4141。与吊坠端朝上成对差值 `[210, −8184, −2]`，第二列主导约 38.97 倍，得到设备 Y 映射及反号的强探索性候选、`counts/g` 探索候选 4092.0、零偏探索候选 +49.0 counts。X/Y/Z 比例候选分别为 4089.5/4092.0/4087.5，但 Y 为手扶数据，整体仍不标记 verified。
- [x] 2026-08-26 的稳定六面静止已用更高质量证据取代 2026-08-25 手扶 Y 轴的临时候选状态。
- [x] 通过六面静止姿态验证当前样机加速度轴顺序、符号与 4096 counts/g 工程比例；版本化证据档案与只读页面已建立。
- [x] 通过八次绕三几何轴的约 360° 正反旋转验证陀螺仪轴顺序、符号与 32.8 counts/(°/s) 工程比例；没有发现证据冲突，但不宣称实验室可溯源精度。
- [ ] 识别 4 字节尾部含义；未知时继续无损保存，不暴露猜测值。
- [x] 已完成多条轻拍录制的人机复核并冻结日常方案：首尾各轻拍一次，默认共同主机时钟，仅在首尾一致且平均偏移绝对值达到 100 ms 时人工应用固定偏移。
- [x] 冻结同步实证方案：5 次独立 90 秒短录制各 5 个轻拍，加 1 次 10 分钟长录制 6 个轻拍；
  用户逐帧确认视频首次接触帧，再选择对应 IMU 峰。
- [x] 增加不修改正式 H5/MKV 的同步实验 JSON、逐帧复核 UI、完整 IMU 窗口、候选峰、来源
  SHA-256、四种同步方法留一法分析和 trailer 多种整数解释统计。
- [x] 历史实验增加 `tap_onset_v3_clustered` 人机协同推荐：同时列出事件首响应、局部峰和时间
  投影点；200 ms 内同一次物理响应的振铃候选先聚合，再在独立响应之间计算置信度。页面显示
  响应显著性、区分比与时间残差，始终由人工确认视频首接触帧与 IMU 首响应。
  首条已校正 5 点的固定偏移回放和线性漂移留一回放均为 5/5 精确命中；跨录制与长录制仍待
  实证。
- [x] 历史 `sync_validation_01` 完成方法验证并促成固定偏移取舍；流程冻结后，其录制、观察 JSON
  和平台联调录制已按用户确认清理，不再作为活动生产证据展示。
- [x] schema 1.2.0 双时间轴已通过两次冒烟和 4 条正式短录制验收：交付 MKV 从 0 开始，
  H5 同时保存媒体时间、录制相对时间和主机单调 PTS，重封装前后逐帧时间一致。
- [~] 罗技 C930c 已通过 20 秒和约 5 分钟 1080p30 录制；现已增加手动曝光写入/回读、约 30 FPS 源输入与约 10 FPS 浏览器预览的独立统计，以及 prod 的 29/27 FPS 前后门禁。短时真实控件与源 FPS 已验收，长时温度和 CPU/GPU 占用仍待测。

## P0：软件闭环

- [x] BLE 直连已在 BlueZ 5.87 上通过两次独立订阅验证：代码每次显式设置 `PreferredBearer=le` 并调用 `Bearer.LE1.Connect()`，停止后不会产生经典蓝牙绑定。
- [x] BLE 连接优先复用 BlueZ 缓存设备 path，仅在对象不存在时扫描；已真实接管
  `LE.Connected=yes`、`ServicesResolved=true` 但不再广播的遗留连接，并收到持续通知。
- [x] FFmpeg 单进程录制/预览、HDF5 收尾与校验已由第二次联合 test 录制验收：320 包、8000 IMU 样本、9572 视频帧，结构与时间戳门禁通过。
- [~] 2026-08-25 第一次联合 test 录制成功生成 H5/MKV，IMU 为 309 包、7725 样本、25.00014 Hz；因 9303 个视频帧含 260 个重复 PTS 而未通过。原件保留，已改为禁用 B 帧与 VFR，等待光线充足条件下重录。
- [x] WebUI 显示视频、实时 FPS、6 轴曲线、磁盘空间、开始/停止；2026-08-26 已由用户确认罗技实时画面恢复。
- [x] 修复 Firefox 中“后端视频帧持续增长但预览区域空白”：MJPEG 每帧显式写入
  `Content-Length`，响应禁用缓存与代理缓冲；有效 JPEG、长连接和最终浏览器画面均已验收。
- [x] 统一不落盘设备预览已由真实硬件验收：非录制时同时显示约 10 FPS 摄像头画面、最近 120 秒六轴曲线、估算频率、最后一包时间、解析错误和回调丢弃；开始录制切换到 1080p30 H5/MKV，结束后自动恢复。
- [~] 已实现预览中切换摄像头只重启 FFmpeg、保留 BLE/IMU 曲线；预览进入录制、结束录制
  均复用同一个 BLE 通知连接和消费任务。浏览器改用跨 FFmpeg 进程的稳定 MJPEG 会话通道，
  未建立通道返回 409，后端增加连接 single-flight；自动化回归已通过，等待真实双摄像头、
  连续 10 次开始/停止录制且无需刷新页面的硬件验收。
- [~] 设备鲁棒性 P0 已实现：设备状态与录制状态分离、预览连接不持有全局锁、释放幂等且
  带硬超时、预览意外断线按 1/2/4 秒重连三次、正式会话断线立即停止并保留 partial、空异常
  转为结构化中文错误；故障注入与真实“设备不可发现”失败路径均已通过，等待真实断线重连验收。
- [x] Uvicorn 增加 5 秒优雅关机上限；浏览器长期 MJPEG/WebSocket 不再把 systemd 重启拖到
  25 秒后 SIGKILL，应用 lifespan 得以正常释放 BLE 和 FFmpeg。
- [x] 本机采集前端增加单活动标签页租约和前后端源码哈希门禁；旧页面不能再控制新版状态机，
  第二标签页只读且不再额外打开 MJPEG 流。
- [ ] 明确设备预览的闲置自动释放时长；当前由用户点击“释放预览设备”，避免后台无提示占用摄像头与 IMU 电量。
- [~] 独立标注应用已支持 fall/non_fall/显式排除、由 fall 区间起点派生 onset、逐区间 impact、
  覆盖空洞提示、同步轻拍排除、GCS `review.json`、revision+generation 乐观锁，以及
  unassigned/in_progress/completed 三态领取与接管；等待云端真实完整标注验收。
- [x] 标注页已增加不可变原始 `capture.h5` 和当前 `review.json` 下载；2026-08-26 已在云端
  通过 IAP 真实下载一条 test 录制，H5 大小 305,167 字节且浏览器下载 SHA-256 与 GCS manifest
  完全一致。`test` 可用于联调与结构展示，但不能生成训练 H5、进入训练快照或同步到
  SOFT3888 `raw`。
- [~] 仅为通过全部门禁的 prod 录制提供版本化 aligned30 H5 生成与下载；实际对象键包含
  review revision 与内容摘要，`review.json.active_export` 指向当前有效版本，重开后不会误用
  旧文件。页面默认跳到约 0.2 秒
  避开相机启动白平衡过渡，但保留第 0 帧和真实逐帧时间戳。当前样机工程校准已验证，下一步
  需要用真实 prod 录制完成同步、标注、完成与云端导出验收。
- [x] 标注端已向九名白名单成员开放录制的永久删除：精确二次确认、`deleting` 状态、
  generation 并发保护和跨制品清理已有自动化测试；训练快照自包含，不阻止源录制删除。
  2026-08-26 已由
  用户在云端永久删除一条一次性 test 录制并确认列表自动切换。`test` 即使残留导出对象也
  禁止下载训练 H5；只允许下载 manifest 已登记的不可变原始 `capture.h5`。
- [x] 发布时无重编码生成可重建 MP4 浏览代理，原始 MKV 始终保留且不修改。
- [~] 正式同步已改为首尾两个来源可追溯锚点和条件式固定偏移：`scale=1`，首尾不一致超过
  100 ms 时重选，估计偏移绝对值达到 100 ms 才建议人工确认应用；等待真实首尾流程验收。
- [x] 当前 9 个 UniKey 已配置为允许名单；本机采集使用参与者下拉，云端标注由 IAP 邮箱映射
  得到可信 UniKey，所有写操作都覆盖浏览器自报身份。
- [x] 摄像头按 udev 序列号与 USB interface 形成稳定 ID，并拒绝 metadata、灰度或不支持目标 profile 的节点。
- [x] 未显式选择摄像头时优先兼容的外接相机，避免设备节点顺序使内置相机被误选。
- [x] 增加 `.partial.*`/空媒体扫描、人工确认隔离、catalog 目录重建和禁止误入训练集；通用修复明确不在当前范围。
- [~] 已实现持久化单 worker 收尾/发布队列、服务重启恢复、5/30/120 秒自动重试、阶段与错误
  展示、人工重新收尾/上传、`prod` 自动发布，以及 manifest-last、大小/SHA-256、能力合同和
  索引回执；自动化已通过，等待真实长录制恢复、并行采集性能和网络中断后的云端验收。
- [x] Python 单测、Ruff 静态检查、前端生产构建与 API 冒烟测试均已通过；真实浏览器交互仍按上方人工验收项跟踪。

## P1：冻结到训练数据

- [x] SOFT3888 已增加 CW12EU-T 训练快照导入器，不影响五个公共数据集的默认构建。
- [x] 独立标注端完成门禁只接受 prod、标注定稿、同步 verified、源哈希不变且加速度/陀螺仪尺度均 verified 的录制。
- [x] 严格 30 Hz 派生保留完整公共有效区间与 activity/onset/impact/exclude，训练窗口负责跳过排除区；不切成大量片段文件。
- [x] 30 Hz 使用原始主机时间和外置同步模型写入独立 `aligned30.h5`，不覆盖约 25 Hz 原始样本；转换边界和无外推已有自动化测试。
- [~] 已实现内容寻址的不可变训练快照 TAR、manifest、逐文件 SHA-256 和 SOFT 端安全解包/拼接
  测试；相同内容复用同一快照，管理员可清理历史。校准门禁已具备真实 profile，仍需第一条
  完整 prod 数据验证端到端快照，参与者划分将在真实数据产生后生成。

## P1：运行与治理

- [x] 增加幂等的 `imu-collector start` 单命令入口：无服务时启动后端、等待健康接口并自动
  打开本机 WebUI；已有健康实例时只打开页面，不创建第二实例或打断录制；保留 `serve` 供
  调试和未来 systemd 用户服务使用。
- [~] 宿主机已验证 `start` 拉起服务后浏览器请求首页、生产资源、配置、设备、录制列表和
  WebSocket 均成功；仍等待用户确认页面视觉显示。
- [x] 已安装可选的 `imu-data-collector.service` 用户服务并完成重启、健康接口和正常退出验收；
  默认保持 disabled、不随登录启动，日常使用 `systemctl --user restart imu-data-collector.service`。
- [x] 增加 `scripts/update-local-capture.sh`：源码更新后拒绝打断 recording/finalizing，会暂存
  构建、核对前后端版本号、原子替换静态目录、重启并等待健康；普通 systemd restart 仅用于
  已构建版本的快速恢复。
- [x] 独立标注应用已部署到 `soft3888-label` VM：HTTPS 托管证书、负载均衡、IAP 身份映射、
  OS Login、WIF/OIDC GitHub Actions、传统 SCP/IAP 上传、原子部署、systemd/API 和生产前端
  均已验收。2026-08-26 生产版本 `4f5df9347181cbdf6529d7a2d48ea14de6f8f806` 通过自动部署、
  公网检查和用户真实浏览器登录，页面正确显示 `xfan0282` 并可播放视频。
- [x] 增加 GitHub Actions：PR 执行后端测试、Ruff、前端双构建和 temporal v3 合同；生产部署只允许
  手动触发、环境审批和 OIDC/WIF 短期身份，禁止保存长期 GCP JSON key，也不在 push 后自动
  发布。
- [ ] 制定参与者知情同意、摄像范围、删除申请和高风险跌倒动作的安全协议。
- [x] 增加每场次正式采集检查清单：身份与安全、IMU 方向/电量、单 BLE 会话、固定曝光、输入
  /预览双 FPS、BLE 频率、剩余空间、首尾轻拍和停止后 H5/MKV 快速验收。
- [ ] 增加剩余容量、预计可录时长、丢包、相机掉帧、BLE 延迟和编码器异常告警。
- [ ] 评估 Ubuntu WSL2：重点验证 USB/BLE 透传；不可假设与原生 Linux 等价。

## 后续里程碑：全面业务规格

- [x] 客户 PDF 暂作为采集协议基线；胸前弹性束带、3 秒静止、跌倒后躺卧 5 秒、A01–A07、
  F01–F05 和安全要求已整理到中文 SOP。
- [x] 当前产品取舍：客户 SOP、动作计划、计时和安全确认只写文档，不进入 WebUI；CSV 暂不
  支持并列为待客户确认；H5+MKV 仍是原始产物。
- [x] 实现全时间轴标注合同：允许显式 `excluded`，定稿时不允许未标注空洞；fall 从首次
  明确失衡到撞击后姿态稳定，onset 由区间起点派生，每个 fall 人工标记独立 impact，定稿后
  仍可修订。

- [ ] 与用户逐项冻结 collection/session/recording/trial/segment 的层级、编号和文件原子单位。
- [ ] 明确参与者、操作者、标注者、复核者角色，以及知情同意、隐私、安全检查和异常备注。
- [ ] 明确协议版本、动作顺序、开始/停止/取消/中断、补录和重录的业务语义。
- [~] 已把采集/发布与同步/标注/导出拆成独立 API、进程和前端构建；仍需真实云端浏览器
  验收全部状态转换。
- [x] 冻结当前快照式修订、三态工作流、同权领取/接管、当前负责人独占编辑、负责人或管理员
  重开、受限硬删除和 `test/prod` 门禁；所有公开写入强制携带 revision，不保存完整编辑历史。
- [x] 训练导出改为不可变版本键与 review 活动指针；校准门禁交叉核对服务器证据、manifest、
  H5 冻结属性和三种源对象 SHA-256 metadata，避免重开后继续下载旧 aligned30。
- [x] 训练快照使用确定性内容指纹，支持“已上传 TAR、清单写入失败”的幂等恢复；每日清理
  无引用旧导出和无清单孤儿快照，并保护所有当前有效导出。

## 后续里程碑：可复用基础设施

- [ ] 抽象 BLE IMU、UVC 摄像头和编码器接口，并提供不依赖真实硬件的回放/模拟测试源。
- [x] 建立机器可读 capture manifest、软件版本、对象键、大小和来源 SHA-256 的统一交接机制。
- [ ] 建立结构化日志、健康事件、性能指标、告警和可导出的单次采集诊断报告。
- [x] 完成 `.partial.*` 扫描、隔离、catalog 重建和幂等收尾；当前明确不做通用恢复副本。
- [~] 完成 GCS/本地对象存储适配、校验上传和按需 H5 缓存；后台任务队列、自动重试和恢复下载
  仍待实现，本地原件未验证前绝不删除。
- [ ] 在原生 Arch 闭环稳定后增加 WSL2 适配层，不把平台核心逻辑绑定到当前 BlueZ/V4L2 实现。

## 已知阻塞/不可伪造结论

- “珠海杰理 JL701N”可以描述已识别的 BLE SoC/平台，不能据此断言整块主板、六轴 MEMS 或 IMU 算法驱动均由珠海杰理提供。
- 供应商所说 30 Hz 与当前 15 秒实测不一致：当前为约每秒一个 400-byte 包、每包 25 帧，初步指向 25 Hz；必须用 30 分钟长测最终确认。
- `MFIMU.pdf` 仅是候选协议参考，不能覆盖设备实测证据。
- 手机 nRF Connect 已验证设备无需绑定即可建立 BLE 连接；电脑端启用 BlueZ 5.87 实验接口并设置 `PreferredBearer=le` 后，也已完成 GATT 枚举与 `0x2AE1` 通知订阅。
- 当前地址样机已有版本化工程校准；缺少 verified profile 的旧录制或其他实物仍只能可信使用
  原始 `int16`，SI 单位必须保持未知，不能从型号名自动继承当前参数。
