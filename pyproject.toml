[project]
name = "imu-data-collector"
version = "0.2.0"
description = "以时间戳为核心的 CW12EU-T IMU 与 UVC 视频数据采集平台"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "bleak>=2.0.0",
    "cachecontrol>=0.14.3",
    "dbus-fast>=5.0.22 ; sys_platform == 'linux'",
    "fastapi>=0.116.0",
    "google-cloud-storage>=3.13.1",
    "google-auth>=2.57.0",
    "h5py>=3.14.0",
    "keyring>=25.6.0",
    "numpy>=2.2.0",
    "pydantic>=2.11.0",
    "pyyaml>=6.0.2",
    "requests>=2.32.0",
    "uvicorn[standard]>=0.35.0",
    "pystray>=0.19.5",
    "pillow>=12.3.0",
    "pyobjc-framework-avfoundation>=11.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-corebluetooth>=11.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-coremedia>=11.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-cocoa>=11.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-quartz>=11.0 ; sys_platform == 'darwin'",
]

[project.scripts]
imu-collector = "imu_data_collector.cli:main"
imu-annotation = "imu_data_collector.annotation_cli:main"
imu-upload-broker = "imu_data_collector.upload_broker:main"

[dependency-groups]
dev = [
    "httpx>=0.28.0",
    "httpx2>=2.0.0",
    "pytest>=8.4.0",
    "pytest-asyncio>=1.1.0",
    "ruff>=0.12.0",
]
package = [
    "pyinstaller==6.22.2",
]

[build-system]
requires = ["uv_build>=0.8.0,<0.9.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
